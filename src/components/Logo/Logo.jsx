import { LogoStyle, LogoStyleLetter } from './Logo.styled';

const Logo = () => {
  return (
    <LogoStyle>
      <span>
        pe
        <LogoStyleLetter>
          <span>t</span>
        </LogoStyleLetter>
        ly
      </span>
    </LogoStyle>
  );
};
export default Logo;
