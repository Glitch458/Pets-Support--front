import { LogoStyle, LogoStyleLetter } from './Logo.styled';

export const Logo = () => {
  return (
    <LogoStyle>
      <span>
        pe
        <LogoStyleLetter>
          <span>t</span>
        </LogoStyleLetter>
        ly
      </span>
    </LogoStyle>
  );
};
