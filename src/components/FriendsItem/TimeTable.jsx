

export const TimeTable = ({shedule}) => {
    // console.log({ shedule })
    // return (<ul>
    //     {shedule && shedule.map(item => <li>{item.day} : {item.from}-{ item.to}</li>)}
    //     </ul>
    // )
}